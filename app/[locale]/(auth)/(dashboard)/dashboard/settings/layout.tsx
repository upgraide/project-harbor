import { SettingsLayout } from "@/modules/dashboard/ui/layouts/settings-layout";

const Layout = ({ children }: { children: React.ReactNode }) => (
  <SettingsLayout>{children}</SettingsLayout>
);

export default Layout;
